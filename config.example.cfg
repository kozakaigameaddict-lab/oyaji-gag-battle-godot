# このファイルをコピーして config/config.cfg を作成し、実際の値を入力してください。
# Copy this file to config/config.cfg and fill in the real values.
# config/config.cfg は .gitignore で除外されています — 絶対にコミットしないでください。

[api]
proxy_url = "https://your-proxy-server-url-here"
